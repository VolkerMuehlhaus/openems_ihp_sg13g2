# Stackup XML editor for gds2openEMS (openEMS FDTD stackup format).
#
# Ported from setupEM's stackupEditor.py/stackup_writer.py/momentum_import.py
# (Palace-format editor, gds2palace-backed) to work against this repo's own
# util_stackup_reader.py (exposed as gds2openEMS.stackup_reader). Per this
# workspace's convention (see CLAUDE.md), this is a duplicated, independently
# maintained copy - not shared/symlinked with setupEM's original.
